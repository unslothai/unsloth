# SPDX-License-Identifier: AGPL-3.0-only
# Copyright 2026-present the Unsloth AI Inc. team. All rights reserved.

# Mac counterpart to studio-ui-smoke.yml. Same Playwright + Chromium
# end-to-end chat UI flow, but on macos-15 (M1) so we catch
# Mac-specific frontend / backend wiring regressions that the Linux
# job would miss (e.g. the Mac Tauri shell loading the same React
# bundle, or the Mac llama.cpp prebuilt's HTTP layer behaving
# differently from the Linux build).
#
# Also carries two phases that used to be their own macOS jobs:
# studio-mac-api-smoke.yml (API + auth) and studio-mac-update-smoke.yml
# (update idempotency + uninstall). All three shared a byte-identical
# `install.sh --local --no-torch` plus assert-llama-loads.sh bootstrap, so a
# single studio/** PR paid three macOS slots and three full installs to test
# three surfaces of the same install. Concurrent macOS jobs are capped at 5
# account-wide and that pool is shared with unslothai/unsloth-zoo, so this was
# the cheapest duplication in the repo to remove.
#
# Phase order matters. Each phase boots on its own port and writes its own
# server log, and the update phase runs LAST because it ends by uninstalling
# and asserting the machine is clean -- that is the teardown for the whole job,
# so nothing needing Unsloth installed may follow it.

name: Mac Studio UI + API + Update CI

on:
  pull_request:
    paths:
      - 'studio/**'
      - 'unsloth/**'
      - 'unsloth_cli/**'
      - 'install.sh'
      - 'pyproject.toml'
      - 'tests/studio/**'
      - '.github/scripts/run-studio-permission-browser.sh'
      - '.github/workflows/studio-mac-ui-smoke.yml'
      # Every server boot in this workflow shells out to that script, so an edit
      # to it changes what this workflow actually runs.
      - '.github/scripts/boot-studio-api-only.sh'
      # Same for the /api/health wait that runs after a boot.
      - '.github/scripts/wait-for-health.sh'
      # The install step in this workflow is `uses:` on that composite action,
      # so an edit to the action changes what this workflow actually runs.
      - '.github/actions/install-unsloth-local/action.yml'
      # The absorbed update phase round-trips scripts/uninstall.sh, which none
      # of the entries above cover.
      - 'scripts/uninstall.sh'
      # No new entries needed for the absorbed API phase: every path
      # studio-mac-api-smoke.yml watched is already covered above, and
      # tests/studio/** covers studio_api_smoke.py itself.
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # Latest-only on branches, never on main. Pushes to main land in bursts, so
  # cancelling superseded runs there throws away the run that would have
  # reported a regression, and the run that writes the shared caches. Same
  # reasoning, and the same incident, as studio-ui-smoke.yml.
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  ui-smoke:
    name: Chat UI, API and Update Tests
    runs-on: macos-15
    # 35 covered the UI phases alone. The absorbed API and update phases each
    # add a server boot plus their own checks, but both reuse this job's single
    # install rather than doing their own, so they cost minutes rather than the
    # ~10 the bootstrap takes. 65 leaves headroom without inviting a hang to sit
    # for an hour holding one of the 5 macOS slots.
    timeout-minutes: 65
    env:
      GGUF_REPO: unsloth/gemma-3-270m-it-GGUF
      GGUF_VARIANT: UD-Q4_K_XL
      GGUF_FILE: gemma-3-270m-it-UD-Q4_K_XL.gguf
      STUDIO_PORT: '18896'
      HF_HOME: ${{ github.workspace }}/hf-cache
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: '22'

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.12'
          cache: 'pip'

      # Cross-OS shared entry. The tree under `hf-cache` is byte-identical on
      # Linux, macOS and Windows, so the key carries no `runner.os`, and
      # enableCrossOsArchive lets Windows (which tars with --force-local) join it.
      - name: Restore HF_HOME for ${{ env.GGUF_REPO }}
        id: cache-hf
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        continue-on-error: true
        with:
          path: hf-cache
          key: hf-${{ env.GGUF_REPO }}-${{ env.GGUF_VARIANT }}-v3
          enableCrossOsArchive: true

      - name: Prime HF_HOME with the GGUF
        id: prime-hf
        if: steps.cache-hf.outputs.cache-hit != 'true' || steps.cache-hf.outcome != 'success'
        # Bounded on purpose. hf-download-with-retry.sh retries forever by
        # design ("the enclosing job's timeout-minutes is the real bound"), so
        # an hf-xet stall used to eat all 65 minutes and take the update and
        # uninstall coverage with it. A step timeout is a step FAILURE, not a
        # cancellation, so the bootstrap below still runs and the job reports
        # what it could test. 25 is roughly 7x the healthy time for a ~200 MB
        # GGUF plus stories260K, so a merely slow download will not trip it.
        timeout-minutes: 25
        env:
          # Withheld on PR: this step runs checked-out PR code; public GGUF still downloads.
          HF_TOKEN: ${{ github.event_name != 'pull_request' && secrets.HF_TOKEN || '' }}
        run: |
          python -m pip install --upgrade huggingface_hub
          mkdir -p hf-cache
          bash .github/scripts/hf-download-with-retry.sh "$GGUF_REPO" "$GGUF_FILE"
          bash .github/scripts/hf-download-with-retry.sh ggml-org/models tinyllamas/stories260K.gguf

      - name: Save HF_HOME for ${{ env.GGUF_REPO }}
        # Save on main only. Caches created on a PR ref are scoped to that
        # merge ref -- per GitHub's docs they "can only be restored by re-runs
        # of the pull request" -- while every PR *can* restore from the default
        # branch. So a PR-scoped save helps almost nothing and competes for the
        # 10GB per-repo budget, and when that budget is exceeded GitHub evicts
        # by least-recently-used, which deletes main's copies that all PRs share.
        # This repo was measured at 33.3GB across 30 caches, 3.3x over, with the
        # same 4.6GB model held four times on four different PR refs and no copy
        # on main at all. That is the thrash loop: PR misses -> downloads ->
        # saves its own copy -> evicts main's -> next PR misses.
        if: always() && github.ref == 'refs/heads/main' && steps.prime-hf.outcome == 'success' && hashFiles('hf-cache/**/*.gguf') != ''
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        with:
          path: hf-cache
          key: hf-${{ env.GGUF_REPO }}-${{ env.GGUF_VARIANT }}-v3
          enableCrossOsArchive: true

      - name: Install Unsloth (--local, --no-torch)
        id: install_unsloth
        if: ${{ !cancelled() }}
        uses: ./.github/actions/install-unsloth-local
        with:
          gh-token: ${{ secrets.GITHUB_TOKEN }}
          # Withheld on PR: this step runs checked-out PR code; public GGUF still downloads.
          hf-token: ${{ github.event_name != 'pull_request' && secrets.HF_TOKEN || '' }}

      - name: Assert llama.cpp loads on this macOS
        id: assert_llama
        # Second half of the bootstrap, and it needs no GGUF: it runs
        # llama-server --version plus a static vtool minimum-OS check.
        if: ${{ !cancelled() && steps.install_unsloth.outcome == 'success' }}
        run: bash .github/scripts/assert-llama-loads.sh

      - name: Install Playwright browsers
        # No --with-deps on Mac: that flag installs Linux apt packages.
        # GitHub-hosted macos-15 ships the system frameworks Chromium
        # needs already.
        # Pinned <1.58 because all 1.55-1.58 drivers ship Node 24 on
        # macOS and intermittently hit 'SyntaxError: Unexpected end
        # of JSON input' in pipeTransport.js. Run 25491698868 showed
        # the crash hitting 100% of three retry attempts -- not a
        # rare race but a hard reproduction. Belt-and-suspenders fix:
        # the test scripts pass --single-process to Chromium (see
        # tests/studio/playwright_chat_ui.py) AND we patch
        # pipeTransport.js below to swallow JSON parse errors instead
        # of crashing the driver Node process. Both together let the
        # in-script retry recover from any residual flakes.
        run: |
          pip install 'playwright>=1.55,<1.58'
          python -m playwright install chromium webkit

      - name: Patch Playwright pipeTransport.js to tolerate malformed JSON
        # In Playwright 1.55-1.58, pipeTransport.js does
        # `JSON.parse(message)` with no try/catch; when Chromium dies
        # mid-write the partial buffer crashes the driver Node
        # process and the test script exits with 'Connection closed
        # while reading from the driver'. Newer Playwright versions
        # added a try/catch upstream. Backport that here.
        run: |
          python - <<'PY'
          import os, re, sys
          import playwright
          driver_dir = os.path.join(os.path.dirname(playwright.__file__), "driver", "package", "lib", "server")
          path = os.path.join(driver_dir, "pipeTransport.js")
          src = open(path).read()
          # Wrap both `this.onmessage.call(null, JSON.parse(...))` sites in try/catch.
          patched = re.sub(
              r"this\.onmessage\.call\(null, JSON\.parse\((message2?)\)\);",
              r"try { this.onmessage.call(null, JSON.parse(\1)); } "
              r"catch (e) { /* swallow malformed JSON from a crashing browser */ }",
              src,
          )
          if patched == src:
              # Already patched, or upstream changed -- either way, don't fail the build.
              print(f"pipeTransport.js: no JSON.parse calls matched at {path}; skipping.")
          else:
              open(path, "w").write(patched)
              print(f"pipeTransport.js: patched JSON.parse calls in {path}")
          PY

      - name: Reset auth + boot Unsloth
        run: |
          # Wipe (not reset-password): the boot below must re-seed a fresh .bootstrap_password.
          bash .github/scripts/boot-studio-api-only.sh --port "$STUDIO_PORT"

      - name: Wait for /api/health
        run: |
          bash .github/scripts/wait-for-health.sh --port "$STUDIO_PORT"

      - name: Pass bootstrap password to the Playwright step
        run: |
          OLD=$(cat ~/.unsloth/studio/auth/.bootstrap_password)
          NEW="CIUi-$(python -c 'import secrets; print(secrets.token_urlsafe(16))')"
          NEW2="CIUi-$(python -c 'import secrets; print(secrets.token_urlsafe(16))')"
          echo "::add-mask::$OLD"
          echo "::add-mask::$NEW"
          echo "::add-mask::$NEW2"
          echo "STUDIO_OLD_PW=$OLD"   >> "$GITHUB_ENV"
          echo "STUDIO_NEW_PW=$NEW"   >> "$GITHUB_ENV"
          echo "STUDIO_NEW2_PW=$NEW2" >> "$GITHUB_ENV"

      - name: Drive the chat UI with Playwright
        env:
          BASE_URL: http://127.0.0.1:18896
          PW_ART_DIR: logs/playwright
          STUDIO_UI_STRICT: '1'
          # macos-15 free runner is 3 vCPU / 7 GB / no Metal-accel
          # available to llama.cpp from CI; gemma-3-270m turn latency
          # has been observed to crowd the 180s default. Triple it.
          STUDIO_UI_TURN_TIMEOUT_MS: '540000'
        # Retry up to 3 times to absorb known macOS free-runner
        # flakes: (1) Playwright Node 24 pipeTransport.js 'Unexpected
        # end of JSON input' crash when the Chromium browser process
        # dies mid-test, (2) Chromium net::ERR_NO_BUFFER_SPACE when the
        # runner's kernel briefly runs out of socket buffers, and (3) a
        # goto 'interrupted by another navigation' when the SPA auth
        # guard redirects mid-navigation. The retry FULLY resets Unsloth
        # (kill, wipe auth, reboot, wait /api/health, re-export
        # bootstrap pw) before re-running the script. A real test failure
        # (assertion / timeout) does NOT match any pattern so it bypasses
        # retry and surfaces immediately.
        run: |
          mkdir -p logs/playwright
          attempt=1
          max_attempts=3
          while : ; do
            set +e
            python tests/studio/playwright_chat_ui.py 2>&1 | tee logs/playwright_attempt_${attempt}.log
            rc=${PIPESTATUS[0]}
            set -e
            if [ "$rc" -eq 0 ]; then
              break
            fi
            if { grep -q "Unexpected end of JSON input"       logs/playwright_attempt_${attempt}.log \
                 || grep -q "ERR_NO_BUFFER_SPACE"             logs/playwright_attempt_${attempt}.log \
                 || grep -q "interrupted by another navigation" logs/playwright_attempt_${attempt}.log; } \
               && [ "$attempt" -lt "$max_attempts" ]; then
              echo "::warning::Playwright flake on attempt ${attempt}; resetting Unsloth and retrying..."
              kill "${STUDIO_PID}" 2>/dev/null || true
              sleep 2
              rm -rf ~/.unsloth/studio/auth
              UNSLOTH_API_ONLY=1 unsloth studio -H 127.0.0.1 -p "$STUDIO_PORT" \
                > "logs/studio_retry_${attempt}.log" 2>&1 &
              STUDIO_PID=$!
              echo "STUDIO_PID=$STUDIO_PID" >> "$GITHUB_ENV"
              for i in $(seq 1 180); do
                if curl -fs "http://127.0.0.1:${STUDIO_PORT}/api/health" > /tmp/health.json \
                   && jq -e '.status == "healthy"' /tmp/health.json >/dev/null; then
                  break
                fi
                sleep 1
              done
              STUDIO_OLD_PW=$(cat ~/.unsloth/studio/auth/.bootstrap_password)
              STUDIO_NEW_PW="CIUi-$(python -c 'import secrets; print(secrets.token_urlsafe(16))')"
              STUDIO_NEW2_PW="CIUi-$(python -c 'import secrets; print(secrets.token_urlsafe(16))')"
              echo "::add-mask::$STUDIO_OLD_PW"
              echo "::add-mask::$STUDIO_NEW_PW"
              echo "::add-mask::$STUDIO_NEW2_PW"
              export STUDIO_OLD_PW STUDIO_NEW_PW STUDIO_NEW2_PW
              attempt=$((attempt + 1))
              sleep 3
              continue
            fi
            exit "$rc"
          done

      - name: Stop Unsloth (chat-ui ends with Shutdown click; this is belt-and-suspenders)
        if: always()
        run: |
          kill "${STUDIO_PID}" 2>/dev/null || true
          sleep 2

      - name: Cross-browser permission controls
        run: |
          bash .github/scripts/run-studio-permission-browser.sh 18895 webkit

      - name: Reset auth + boot Unsloth for extra UI tests (port 18897)
        run: |
          bash .github/scripts/boot-studio-api-only.sh \
            --port 18897 --log logs/studio_extra.log --pid-var STUDIO_EXTRA_PID

      - name: Wait for /api/health on 18897
        run: |
          bash .github/scripts/wait-for-health.sh \
            --port 18897 --log logs/studio_extra.log --tmp /tmp/health2.json

      - name: Pass bootstrap pw for extra UI test
        run: |
          OLD=$(cat ~/.unsloth/studio/auth/.bootstrap_password)
          NEW="CIUiExtra-$(python -c 'import secrets; print(secrets.token_urlsafe(16))')"
          echo "::add-mask::$OLD"
          echo "::add-mask::$NEW"
          echo "STUDIO_EXTRA_OLD_PW=$OLD" >> "$GITHUB_ENV"
          echo "STUDIO_EXTRA_NEW_PW=$NEW" >> "$GITHUB_ENV"

      - name: Drive Compare/Recipes/Export/Unsloth/Settings with Playwright
        env:
          BASE_URL: http://127.0.0.1:18897
          STUDIO_OLD_PW: ${{ env.STUDIO_EXTRA_OLD_PW }}
          STUDIO_NEW_PW: ${{ env.STUDIO_EXTRA_NEW_PW }}
          PW_ART_DIR: logs/playwright_extra
          STUDIO_UI_STRICT: '1'
          # See "Drive the chat UI" step.
          STUDIO_UI_TURN_TIMEOUT_MS: '540000'
          GGUF_REPO: ${{ env.GGUF_REPO }}
          GGUF_VARIANT: ${{ env.GGUF_VARIANT }}
        # Same flake-retry shape as "Drive the chat UI with Playwright" -- catches
        # pipeTransport JSON crash, ERR_NO_BUFFER_SPACE, and nav interrupts.
        run: |
          mkdir -p logs/playwright_extra
          attempt=1
          max_attempts=3
          while : ; do
            set +e
            python tests/studio/playwright_extra_ui.py 2>&1 | tee logs/playwright_extra_attempt_${attempt}.log
            rc=${PIPESTATUS[0]}
            set -e
            if [ "$rc" -eq 0 ]; then
              break
            fi
            if { grep -q "Unexpected end of JSON input"       logs/playwright_extra_attempt_${attempt}.log \
                 || grep -q "ERR_NO_BUFFER_SPACE"             logs/playwright_extra_attempt_${attempt}.log \
                 || grep -q "interrupted by another navigation" logs/playwright_extra_attempt_${attempt}.log; } \
               && [ "$attempt" -lt "$max_attempts" ]; then
              echo "::warning::Playwright flake on attempt ${attempt}; resetting Unsloth and retrying..."
              kill "${STUDIO_EXTRA_PID}" 2>/dev/null || true
              sleep 2
              rm -rf ~/.unsloth/studio/auth
              UNSLOTH_API_ONLY=1 unsloth studio -H 127.0.0.1 -p 18897 \
                > "logs/studio_extra_retry_${attempt}.log" 2>&1 &
              STUDIO_EXTRA_PID=$!
              echo "STUDIO_EXTRA_PID=$STUDIO_EXTRA_PID" >> "$GITHUB_ENV"
              for i in $(seq 1 180); do
                if curl -fs "http://127.0.0.1:18897/api/health" > /tmp/health2.json \
                   && jq -e '.status == "healthy"' /tmp/health2.json >/dev/null; then
                  break
                fi
                sleep 1
              done
              STUDIO_OLD_PW=$(cat ~/.unsloth/studio/auth/.bootstrap_password)
              STUDIO_NEW_PW="CIUiExtra-$(python -c 'import secrets; print(secrets.token_urlsafe(16))')"
              echo "::add-mask::$STUDIO_OLD_PW"
              echo "::add-mask::$STUDIO_NEW_PW"
              export STUDIO_OLD_PW STUDIO_NEW_PW
              attempt=$((attempt + 1))
              sleep 3
              continue
            fi
            exit "$rc"
          done

      - name: Stop second Unsloth
        if: always()
        run: |
          kill "${STUDIO_EXTRA_PID}" 2>/dev/null || true
          sleep 2

      # ---------------------------------------------------------------
      # Tab-capability phase. The assertion is that the Train row SPINS rather than
      # greying out while the hardware verdict is unmeasured.
      #
      # It gets its own boot on a cold port anyway, and no "wait for healthy" step
      # where every other phase has one, because a settled backend is the wrong
      # starting state to walk the tabs from. But do not read that as the phase
      # keeping the provisional window open for the script: it does not, and this
      # comment used to claim it did. `hardware_detecting` covers stage 0 of the warm
      # only, and on this runner's --no-torch install with no MLX present that stage
      # is a failed `import torch` plus one failed metadata lookup, so the verdict
      # settles inside a second of the port binding -- while Playwright is still
      # launching Chromium. Boot ordering buys the first /api/health probe a
      # provisional reply and nothing past it.
      #
      # So the script does not depend on the window being open. It holds the
      # browser's own /api/health at hardware_detecting=true and asserts against
      # that, which is open for as long as it needs on any host. The real warm is
      # still sampled when it happens to still be running, and still fails on a real
      # grey-out, but nothing is required of it.
      # ---------------------------------------------------------------
      - name: Reset auth + boot Unsloth for the tab-capability smoke (port 18893)
        run: |
          bash .github/scripts/boot-studio-api-only.sh \
            --port 18893 --log logs/studio_tabs.log --pid-var STUDIO_TABS_PID

      - name: Pass bootstrap pw for the tab-capability smoke
        run: |
          # Wait for the password file, not for health: health is the thing under test.
          for i in $(seq 1 180); do
            [ -f ~/.unsloth/studio/auth/.bootstrap_password ] && break
            sleep 1
          done
          OLD=$(cat ~/.unsloth/studio/auth/.bootstrap_password)
          echo "::add-mask::$OLD"
          echo "STUDIO_TABS_OLD_PW=$OLD" >> "$GITHUB_ENV"

      - name: Drive the Train/Video capability gate with Playwright
        env:
          BASE_URL: http://127.0.0.1:18893
          STUDIO_OLD_PW: ${{ env.STUDIO_TABS_OLD_PW }}
          PW_ART_DIR: logs/playwright_mac_tabs
          # The default 330s outlives the launcher's 300s startup grace, which is the
          # point when a Tauri shell is driving the backend. Nothing here runs that
          # watchdog (this phase boots `unsloth studio` directly), so the full window
          # would spend five macOS-runner minutes proving something this job cannot
          # observe. The watchdog itself is covered by the Rust tests in
          # studio/src-tauri/src/commands.rs. What is unique to this phase is the
          # first-paint tab assertion, so keep the liveness poll just long enough to
          # cover the warm and the tab walk.
          STUDIO_MAC_SURVIVAL_S: '120'
        run: |
          mkdir -p logs/playwright_mac_tabs
          python tests/studio/playwright_mac_tab_capabilities.py

      - name: Stop tab-capability Unsloth
        if: always()
        run: |
          kill "${STUDIO_TABS_PID}" 2>/dev/null || true
          sleep 2

      # ---------------------------------------------------------------
      # API + auth phase, absorbed from studio-mac-api-smoke.yml.
      # Runs on 18894: 18896 and 18897 were used by the UI phases above and
      # 18895 by the permission-browser script, so a fresh port keeps this
      # phase independent of whether any of those shut down cleanly.
      # ---------------------------------------------------------------
      - name: Install pyjwt for the JWT-expiry forge test
        run: pip install 'pyjwt>=2.6'

      - name: Reset auth + boot Unsloth (API-only, port 18894)
        run: |
          # Wipe (not reset-password): the boot below must re-seed a fresh .bootstrap_password.
          bash .github/scripts/boot-studio-api-only.sh \
            --port 18894 --log logs/studio_api.log --pid-var STUDIO_API_PID

      - name: Wait for /api/health on 18894
        run: |
          bash .github/scripts/wait-for-health.sh \
            --port 18894 --log logs/studio_api.log --tmp /tmp/health_api.json

      - name: Pass bootstrap password + rotated targets to the API test
        run: |
          OLD=$(cat ~/.unsloth/studio/auth/.bootstrap_password)
          NEW="ApiSmoke-$(python -c 'import secrets; print(secrets.token_urlsafe(16))')"
          NEW2="ApiSmoke-$(python -c 'import secrets; print(secrets.token_urlsafe(16))')"
          echo "::add-mask::$OLD"
          echo "::add-mask::$NEW"
          echo "::add-mask::$NEW2"
          echo "STUDIO_OLD_PW=$OLD"  >> "$GITHUB_ENV"
          echo "STUDIO_NEW_PW=$NEW"  >> "$GITHUB_ENV"
          echo "STUDIO_NEW2_PW=$NEW2" >> "$GITHUB_ENV"

      - name: Run Unsloth API & Auth tests
        env:
          BASE_URL: http://127.0.0.1:18894
          STUDIO_AUTH_DIR: /Users/runner/.unsloth/studio/auth
        run: python tests/studio/studio_api_smoke.py

      - name: Stop API-phase Unsloth
        if: always()
        run: |
          kill "${STUDIO_API_PID}" 2>/dev/null || true
          sleep 2

      # ---------------------------------------------------------------
      # Update + uninstall phase, absorbed from studio-mac-update-smoke.yml.
      # Same install, same assert-llama-loads.sh, so it was a third macOS
      # runner doing the same bootstrap to check that `update` is a no-op.
      #
      # Runs last on purpose: it ends by uninstalling and asserting the machine
      # is clean, which is the teardown for this whole job. Nothing may follow
      # it that needs Unsloth installed. Its server boots on 18891 and logs to
      # studio_update.log so it cannot clobber the UI or API phases above.
      #
      # Every step below carries an explicit condition because a step with no
      # `if:` gets an implicit `success()`, so one flaky Playwright failure in a
      # phase above would silently skip this whole phase. As its own workflow it
      # ran regardless, and it is the only Mac coverage of `studio update` and
      # scripts/uninstall.sh. `!cancelled()` overrides the implicit success()
      # while the step-outcome checks still demand a working install to test
      # against. `${{ }}` is required: a bare leading `!` is a YAML tag.
      # ---------------------------------------------------------------
      - name: First update should be a no-op (prebuilt already validated)
        id: update_first
        if: ${{ !cancelled() && steps.install_unsloth.outcome == 'success' && steps.assert_llama.outcome == 'success' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Withheld on PR: this step runs checked-out PR code; public GGUF still downloads.
          HF_TOKEN: ${{ github.event_name != 'pull_request' && secrets.HF_TOKEN || '' }}
        run: |
          set -o pipefail
          unsloth studio update --local 2>&1 | tee logs/update.log
          if grep -q "falling back to source build" logs/update.log; then
            echo "::error::studio update fell back to source-build llama.cpp on Mac."
            grep -E "llama-prebuilt|llama.cpp" logs/update.log | tail -60
            exit 1
          fi
          if ! grep -qE "prebuilt up to date and validated|prebuilt installed and validated" logs/update.log; then
            echo "::error::no prebuilt up-to-date marker in update.log."
            grep -E "llama-prebuilt|llama.cpp" logs/update.log | tail -60
            exit 1
          fi
          echo "update path took the prebuilt fast path"

      # Chained on the first update: "also a no-op" says nothing if the first
      # one never produced a clean result. Success here implies the bootstrap
      # gate above passed too, so it does not need repeating.
      - name: Second update must also be a no-op
        id: update_second
        if: ${{ !cancelled() && steps.update_first.outcome == 'success' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Withheld on PR: this step runs checked-out PR code; public GGUF still downloads.
          HF_TOKEN: ${{ github.event_name != 'pull_request' && secrets.HF_TOKEN || '' }}
        run: |
          set -o pipefail
          unsloth studio update --local 2>&1 | tee logs/update2.log
          grep -q "falling back to source build" logs/update2.log && {
              echo "::error::second update fell back to source build on Mac"
              tail -60 logs/update2.log; exit 1; } || true
          grep -qE "prebuilt up to date and validated|prebuilt installed and validated" logs/update2.log
          echo "second update was clean"

      - name: Boot Unsloth briefly to confirm the install is still usable
        if: ${{ !cancelled() && steps.update_second.outcome == 'success' }}
        run: |
          mkdir -p logs
          UNSLOTH_API_ONLY=1 unsloth studio -H 127.0.0.1 -p 18891 \
            > logs/studio_update.log 2>&1 &
          PID=$!
          HEALTHY=""
          for i in $(seq 1 60); do
            if curl -fs http://127.0.0.1:18891/api/health > /tmp/health.json; then
              if python3 -c "import json,sys; d=json.load(open('/tmp/health.json')); sys.exit(0 if d.get('status')=='healthy' else 1)"; then
                HEALTHY=1
                break
              fi
            fi
            sleep 1
          done
          if [ -z "$HEALTHY" ]; then
            echo "Unsloth failed to come up after \`update\`"
            tail -200 logs/studio_update.log
            kill "$PID" 2>/dev/null || true
            exit 1
          fi
          kill "$PID" 2>/dev/null || true
          echo "post-update Unsloth /api/health OK"

      - name: Uninstall and verify clean
        # Gated on the install alone, not on the update steps: uninstall is a
        # separate surface, and a broken `studio update` must not hide whether
        # scripts/uninstall.sh still leaves the machine clean.
        if: ${{ !cancelled() && steps.install_unsloth.outcome == 'success' }}
        # Round-trip through scripts/uninstall.sh on real macOS. As a side
        # effect this exercises the macOS-only .app bundle + Launch Services
        # removal path (~/Applications/Unsloth Studio.app, lsregister -u)
        # which is not testable from a Linux runner. Skips gracefully if
        # scripts/uninstall.sh has not landed yet (lets this workflow merge
        # before #5497).
        run: |
          set -o pipefail
          if [ ! -f scripts/uninstall.sh ]; then
            echo "scripts/uninstall.sh not present in this tree; skipping round-trip"
            : > logs/uninstall.log
            exit 0
          fi
          sh scripts/uninstall.sh 2>&1 | tee logs/uninstall.log
          leak=0
          for p in \
            "$HOME/.unsloth/studio" \
            "$HOME/.local/share/unsloth" \
            "$HOME/Applications/Unsloth Studio.app" \
            "$HOME/Desktop/Unsloth Studio.app" \
            "$HOME/.local/bin/unsloth"; do
            if [ -e "$p" ] || [ -L "$p" ]; then
              echo "::error::leak: $p"
              leak=$((leak + 1))
            fi
          done
          [ "$leak" -eq 0 ] || exit 1
          sh scripts/uninstall.sh 2>&1 | tail -5
          sh scripts/uninstall.sh 2>&1 | tail -5
          echo "PASS: mac install -> update -> uninstall round-trip clean"

      - name: Upload Playwright artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: mac-studio-ui-smoke-artifacts
          path: |
            logs/studio.log
            logs/studio_extra.log
            logs/studio_api.log
            logs/studio_update.log
            logs/update.log
            logs/update2.log
            logs/uninstall.log
            logs/install.log
            logs/playwright
            logs/playwright-permissions-*
            logs/playwright_extra
            logs/studio-permissions-*.log
            logs/studio_tabs.log
            logs/playwright_mac_tabs
          # 1 day, matching the repository-wide cap. A larger value here is
          # silently clamped to that cap today, so stating 7 only invites the
          # artifacts to re-inflate if the cap is ever raised.
          retention-days: 1
