# SPDX-License-Identifier: AGPL-3.0-only
# Copyright 2026-present the Unsloth AI Inc. team. All rights reserved.

# Windows counterpart to studio-update-smoke.yml /
# studio-mac-update-smoke.yml. Verifies that on the FREE
# windows-latest runner:
#
#   1. install.ps1 --local --no-torch installs Unsloth AND auto-fetches
#      the prebuilt llama.cpp Windows binary (app-<tag>-windows-x64-cpu
#      from unslothai/llama.cpp). Hitting the source-build fallback is
#      treated as an Unsloth bug -- Unsloth must always pick the
#      prebuilt on Windows.
#   2. unsloth studio update --local is idempotent. Two consecutive
#      runs both report "prebuilt up to date and validated", no
#      source-build fallback. The CLI's _find_setup_script picks
#      setup.ps1 on Windows automatically.
#   3. The installed Unsloth still boots and /api/health returns
#      healthy after the update path.

name: Windows Unsloth Update CI

on:
  pull_request:
    paths:
      - 'install.ps1'
      - 'scripts/uninstall.ps1'
      - 'tests/studio/test_uninstall_*.ps1'
      - 'tests/studio/test_psmodulepath_normalization.ps1'
      - 'studio/setup.ps1'
      - 'studio/setup.bat'
      - 'studio/install_python_stack.py'
      - 'studio/install_llama_prebuilt.py'
      - 'studio/backend/requirements/**'
      - 'unsloth_cli/commands/studio.py'
      - 'pyproject.toml'
      - '.github/workflows/studio-windows-update-smoke.yml'
  push:
    branches: [main, pip]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  update-idempotency:
    name: Unsloth Updating Tests
    runs-on: windows-latest
    timeout-minutes: 30
    defaults:
      run:
        shell: bash
    env:
      # Force UTF-8 for stdio (Windows defaults to cp1252; hf
      # download / Unsloth CLI print "✓" checkmarks and crash
      # otherwise).
      PYTHONIOENCODING: utf-8
      PYTHONUTF8: '1'
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false

      # This workflow triggers on uninstall.ps1; neither test really uninstalls.
      - name: uninstall.ps1 unit tests (argument guard, dual-install icon preserve)
        shell: pwsh
        run: |
          # Propagate each child test's failure before continuing.
          pwsh -NoProfile -File tests/studio/test_uninstall_arg_guard.ps1
          if ($LASTEXITCODE) { exit $LASTEXITCODE }
          pwsh -NoProfile -File tests/studio/test_uninstall_dual_install_icon.ps1
          if ($LASTEXITCODE) { exit $LASTEXITCODE }

      # Both entry points prepend the 5.1 system module dir, and Refresh-Environment
      # must not reload PSModulePath back over it before the uv installer runs.
      - name: PSModulePath normalization unit tests
        shell: pwsh
        run: pwsh -NoProfile -File tests/studio/test_psmodulepath_normalization.ps1

      # The guard is self-hosting, so launching it from powershell.exe covers
      # Windows PowerShell 5.1: the shell the README one-liner lands in, handled
      # nowhere else in CI, and it differs from pwsh 7 on native stderr.
      - name: uninstall.ps1 argument guard under Windows PowerShell 5.1
        shell: powershell
        run: |
          powershell -NoProfile -File tests/studio/test_uninstall_arg_guard.ps1
          if ($LASTEXITCODE) { exit $LASTEXITCODE }

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0
        with:
          node-version: '22'

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: '3.12'
          # Don't cache pip: install.ps1 + setup.ps1 go through uv
          # and never populate ~/.cache/pip; setup-python's post-step
          # then fatal-errors with "Cache folder path is retrieved
          # for pip but doesn't exist on disk".

      - name: Pre-install Windows tweaks (npm 11 + Defender exclusions)
        shell: pwsh
        # Two surgical fixes against measured Windows-only install
        # waste (vs Mac/Linux on the same SHA):
        #
        # (1) npm. setup.ps1's Get-NodeDecision requires Node 22.12+
        #     (or 20.19+ / 23+) AND npm >=11 because Vite 8 needs both.
        #     actions/setup-node@v4 with `node-version: '22'` lands
        #     Node 22.22.2 + the npm 10.9.7 it bundles, so the decision
        #     is "bundled" and setup.ps1 downloads an isolated Node (~30
        #     MB) we don't need on a runner that already has a fine Node.
        #     `npm install -g npm@^11` updates the runner's npm in-place
        #     in ~5 s, flipping the decision to "system" so setup.ps1
        #     reuses the existing Node with no download.
        #
        # (2) Defender. windows-latest's real-time scan opens / hashes
        #     every file Unsloth writes during install (Vite output =
        #     thousands of small chunks, uv pip = wheel-extraction =
        #     thousands of small files). The latency dominates the
        #     200 s frontend build and the 90 s deps install. Adding
        #     ExclusionPath entries for the directories the install
        #     writes to drops per-file open latency from ~ms to ~us.
        #     Add-MpPreference needs admin; the runneradmin user has
        #     it, but wrap in try/catch so a permission flake leaves
        #     the install otherwise unaffected.
        run: |
          $ProgressPreference = 'SilentlyContinue'
          Write-Host "npm version before upgrade: $(npm -v)"
          npm install -g 'npm@^11' 2>&1 | Out-Host
          Write-Host "npm version after upgrade: $(npm -v)"
          # NOTE: do NOT pre-create these directories before adding the
          # exclusion -- creating an empty studio/frontend/dist trips
          # setup.ps1 line 1281-1296's mtime-based "is the frontend
          # stale?" check into "up to date, skip rebuild", because the
          # newly-created dist's mtime is younger than every source
          # file. Unsloth then boots with an empty dist and 500s on
          # GET / with FileNotFoundError: dist\index.html. See run
          # 25546676715 / job 74984469728.
          # Add-MpPreference accepts paths that do not yet exist; the
          # exclusion is registered and applies when the path
          # materialises.
          foreach ($p in @(
            "$env:USERPROFILE\.unsloth",
            "$env:USERPROFILE\AppData\Local\uv",
            "$env:GITHUB_WORKSPACE\studio\frontend\node_modules",
            "$env:GITHUB_WORKSPACE\studio\frontend\dist"
          )) {
            try {
              Add-MpPreference -ExclusionPath $p -ErrorAction Stop
              Write-Host "Defender exclusion added: $p"
            } catch {
              Write-Host "Defender exclusion skipped ($($_.Exception.Message)): $p"
            }
          }

      - name: Install Unsloth (--local, --no-torch)
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Withheld on PR: this step runs checked-out PR code; public GGUF still downloads.
          HF_TOKEN: ${{ github.event_name != 'pull_request' && secrets.HF_TOKEN || '' }}
        run: |
          New-Item -ItemType Directory -Force -Path logs | Out-Null
          # *>&1 captures Write-Host (Information stream) output;
          # plain 2>&1 does not. setup.ps1 emits "prebuilt installed
          # and validated" via Write-Host, and we grep for that.
          $ProgressPreference = 'SilentlyContinue'
          & ./install.ps1 --local --no-torch *>&1 | Tee-Object -FilePath logs/install.log

      - name: Assert install.ps1 used the Windows llama.cpp prebuilt
        run: |
          # Filesystem-based check (setup.ps1's stream output isn't
          # captured back through the parent pipeline).
          LLAMA_DIR=~/.unsloth/llama.cpp
          INFO="$LLAMA_DIR/UNSLOTH_PREBUILT_INFO.json"
          BIN="$LLAMA_DIR/build/bin/Release/llama-server.exe"
          if grep -q "falling back to source build" logs/install.log; then
            echo "::error::install.ps1 fell back to source-build llama.cpp on Windows."
            grep -E "llama-prebuilt|llama.cpp" logs/install.log | tail -60
            exit 1
          fi
          if [ ! -f "$INFO" ]; then
            echo "::error::no UNSLOTH_PREBUILT_INFO.json at $INFO."
            ls -la "$LLAMA_DIR" || true
            exit 1
          fi
          if [ ! -f "$BIN" ]; then
            echo "::error::no llama-server.exe at $BIN."
            ls -la "$LLAMA_DIR/build/bin" || true
            exit 1
          fi
          echo "install.ps1 installed the Windows prebuilt llama.cpp:"
          cat "$INFO"

      - name: Add Unsloth shim to GITHUB_PATH
        run: |
          SHIM_DIR=~/.unsloth/studio/bin
          if [ ! -f "$SHIM_DIR/unsloth.exe" ]; then
            echo "::error::unsloth.exe shim not found at $SHIM_DIR"
            ls -la ~/.unsloth/studio/ || true
            exit 1
          fi
          cygpath -w "$SHIM_DIR" >> "$GITHUB_PATH"

      - name: First update should be a no-op (prebuilt already validated)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Withheld on PR: this step runs checked-out PR code; public GGUF still downloads.
          HF_TOKEN: ${{ github.event_name != 'pull_request' && secrets.HF_TOKEN || '' }}
        run: |
          set -o pipefail
          unsloth studio update --local 2>&1 | tee logs/update.log
          if grep -q "falling back to source build" logs/update.log; then
            echo "::error::studio update fell back to source-build llama.cpp on Windows."
            grep -E "llama-prebuilt|llama.cpp" logs/update.log | tail -60
            exit 1
          fi
          if ! grep -qE "prebuilt up to date and validated|prebuilt installed and validated" logs/update.log; then
            echo "::error::no prebuilt up-to-date marker in update.log."
            grep -E "llama-prebuilt|llama.cpp" logs/update.log | tail -60
            exit 1
          fi
          echo "update path took the prebuilt fast path"

      - name: Update must keep the --no-torch install GGUF-only
        run: |
          # `unsloth studio update` exports no UNSLOTH_NO_TORCH, so setup.ps1 has
          # to recover the mode from the install manifest. Without that it reads
          # the missing torch as a stale venv and tries to delete the venv it is
          # running out of, and the shared dependency pass pulls torch back in.
          # The skip line only prints when the dependency pass actually runs, so
          # don't demand it if the fast path short-circuited that pass.
          if grep -q "running ordered dependency installation" logs/update.log \
             && ! grep -q "skipping direct PyTorch and Triton installation (no-torch mode)" logs/update.log; then
            echo "::error::studio update left no-torch mode; it would reinstall PyTorch."
            grep -iE "no-torch|stale venv|PyTorch" logs/update.log | tail -40
            exit 1
          fi
          PY="$HOME/.unsloth/studio/unsloth_studio/Scripts/python.exe"
          if [ ! -f "$PY" ]; then
            echo "::error::studio venv interpreter missing at $PY"
            exit 1
          fi
          if "$PY" -c "import torch" 2>/dev/null; then
            echo "::error::torch was reinstalled into the --no-torch venv."
            exit 1
          fi
          echo "update preserved no-torch mode"

      - name: Second update must also be a no-op
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Withheld on PR: this step runs checked-out PR code; public GGUF still downloads.
          HF_TOKEN: ${{ github.event_name != 'pull_request' && secrets.HF_TOKEN || '' }}
        run: |
          set -o pipefail
          unsloth studio update --local 2>&1 | tee logs/update2.log
          grep -q "falling back to source build" logs/update2.log && {
              echo "::error::second update fell back to source build on Windows"
              tail -60 logs/update2.log; exit 1; } || true
          grep -qE "prebuilt up to date and validated|prebuilt installed and validated" logs/update2.log
          echo "second update was clean"

      - name: An update that must replace the unsloth package (issue #7697)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HF_TOKEN: ${{ github.event_name != 'pull_request' && secrets.HF_TOKEN || '' }}
        run: |
          set -o pipefail
          # Every update above is a no-op, so pip never rewrites Scripts/unsloth.exe
          # and the lock this guards is never reached. Force the replacement.
          PY="$HOME/.unsloth/studio/unsloth_studio/Scripts/python.exe"
          SCRIPTS="$HOME/.unsloth/studio/unsloth_studio/Scripts"
          SHIM="$HOME/.unsloth/studio/bin/unsloth.exe"
          test -f "$SHIM" || { echo "::error::no launcher at $SHIM"; exit 1; }
          # With build isolation, not without. The build backend and setuptools-scm
          # are build requirements, not runtime deps of the --no-torch install, so
          # --no-build-isolation would rest on whatever happens to be in the Studio
          # venv -- and if that ever stops including setuptools, this fails here and
          # the lock coverage below never runs at all.
          "$PY" -m pip install --no-deps --force-reinstall . 2>&1 | tail -5
          test -f "$SCRIPTS/unsloth.exe" || { echo "::error::pip left no unsloth.exe"; exit 1; }

          # The reinstall above replaced the editable install with a site-packages
          # copy, so the CLI can no longer derive the checkout from __file__ and
          # --local would exit at the argument guard without ever reaching the lock.
          # A Windows path: MSYS rewrites arguments that look like paths but not
          # environment variables, so the native python would not resolve /d/a/...
          STUDIO_LOCAL_REPO="$(cygpath -w "$PWD")"
          export STUDIO_LOCAL_REPO
          test -f "$PWD/pyproject.toml" || { echo "::error::not a checkout: $PWD"; exit 1; }

          # Through the launcher, which is a second link to the same binary. Windows
          # locks the entry an image was launched from, not the file, so this leaves
          # the venv copy replaceable.
          "$SHIM" studio update --local 2>&1 | tee logs/update_replace.log
          if grep -qiE "WinError 32|could not rename unsloth\.exe|being used by another process" \
                 logs/update_replace.log; then
            echo "::error::the launcher path hit the unsloth.exe lock (issue #7697)."
            grep -iE "WinError|rename|permission" logs/update_replace.log | tail -30
            exit 1
          fi
          # Positive, not the absence of an error message: before the export
          # above, run 30751891659 passed this whole step while the update
          # resolved its "local repo" to
          # ...\unsloth_studio\Lib\site-packages and overlaid that, so it was
          # reinstalling the venv over itself rather than the checkout. Nothing
          # here noticed, because every assertion below is about the state of
          # unsloth.exe and all of them held. Assert the checkout was the thing
          # installed. setup prints the path it overlays, wrapping long ones onto
          # the next line, so match the file rather than a single line, and fold
          # case and separators so a resolve() that canonicalises either does not
          # fail the step over a spelling.
          NEEDLE="$(printf '%s' "$STUDIO_LOCAL_REPO" | tr 'A-Z\\' 'a-z/')"
          HAYSTACK="$(tr -d '\r' < logs/update_replace.log | tr 'A-Z\\' 'a-z/')"
          case "$HAYSTACK" in
            *"$NEEDLE"*) ;;
            *)
              echo "::error::the update did not install from the checkout ($STUDIO_LOCAL_REPO)."
              grep -iE "local repo|site-packages" logs/update_replace.log | tail -10
              exit 1
              ;;
          esac
          # The update renames unsloth.exe aside so pip can replace it. If pip then
          # reinstalls nothing, the renamed copy is the only one there is, so a
          # cleanup that just deletes it leaves no CLI behind.
          if [ ! -s "$SCRIPTS/unsloth.exe" ]; then
            echo "::error::the update left no usable unsloth.exe in the venv."
            ls -la "$SCRIPTS" | grep -i unsloth || true
            exit 1
          fi
          if ls "$SCRIPTS"/*.deleteme >/dev/null 2>&1; then
            echo "::error::a .deleteme orphan survived the update."
            ls -la "$SCRIPTS"/*.deleteme
            exit 1
          fi
          # Run the venv copy itself, not the launcher: that is the file pip
          # replaced and the one the cleanup could have deleted, so existence
          # alone is not enough -- it has to still be a working image. Not
          # `import unsloth`: this install is --no-torch by design (the step
          # above asserts torch is absent), and the package refuses to import
          # without it, so that would fail on a perfectly good install. And a
          # bare import from the repo root resolves to the checked-out source
          # tree rather than the installed copy, so it would not have been
          # testing this at all. desktop-capabilities needs neither.
          CAPS="$("$SCRIPTS/unsloth.exe" studio desktop-capabilities --json)" || {
            echo "::error::the replaced unsloth.exe does not run."
            exit 1
          }
          echo "$CAPS"
          echo "$CAPS" | jq -e '.studio_install_ok == true' >/dev/null || {
            echo "::error::the CLI runs but reports the install as damaged."
            exit 1
          }
          echo "an update that replaces the unsloth package works through the launcher"

      - name: The same update from the venv copy refuses before it breaks the install (issue #7697)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HF_TOKEN: ${{ github.event_name != 'pull_request' && secrets.HF_TOKEN || '' }}
        run: |
          set -o pipefail
          # The step above only ever drives the shim, so nothing here covers the
          # path a user hits by running Scripts\unsloth.exe directly -- which is
          # what issue #7697 is about, and what the desktop app did before this PR.
          SCRIPTS="$HOME/.unsloth/studio/unsloth_studio/Scripts"
          PY="$SCRIPTS/python.exe"
          SHIM_UPD="$HOME/.unsloth/studio/bin/unsloth.exe"
          # Build isolation here for the same reason as the step above.
          "$PY" -m pip install --no-deps --force-reinstall . 2>&1 | tail -3

          # The step above already reinstalled unsloth non-editably, so the CLI now
          # runs out of site-packages and cannot derive the checkout from __file__.
          # Without this, `--local` exits at the argument guard and never reaches
          # the lock, which is what this step exists to exercise. A Windows path:
          # MSYS rewrites arguments that look like paths, but not environment
          # variables, so the native python would not resolve an /d/a/... one.
          STUDIO_LOCAL_REPO="$(cygpath -w "$PWD")"
          export STUDIO_LOCAL_REPO
          test -f "$PWD/pyproject.toml" || { echo "::error::not a checkout: $PWD"; exit 1; }

          # Before this PR the venv-launched update was destructive, not merely
          # failing: pip uninstalls before it installs, so it removed unsloth_cli
          # and only then hit the locked stub, leaving an unsloth.exe that starts
          # and raises ModuleNotFoundError. It now refuses before pip runs, so the
          # update fails and the install survives intact.
          set +e
          "$SCRIPTS/unsloth.exe" studio update --local > logs/update_venv.log 2>&1
          RC=$?
          set -e
          echo "venv-launched update exited $RC"
          tail -50 logs/update_venv.log

          # Guard against the step passing for the wrong reason: exiting at an
          # argument guard would satisfy the assertions below while testing none
          # of them.
          if grep -q "needs an Unsloth checkout to install from" logs/update_venv.log; then
            echo "::error::the update never started; this step proved nothing."
            exit 1
          fi
          if [ "$RC" -eq 0 ]; then
            echo "::error::the venv-launched update succeeded, so the rename no longer"
            echo "fails and this step has stopped covering the lock it exists for."
            exit 1
          fi
          if ! grep -q "Nothing has been removed" logs/update_venv.log; then
            echo "::error::it did not stop before pip; the install may be destroyed."
            exit 1
          fi
          # The destruction this prevents: pip must not have started, so no
          # uninstall line may appear.
          if grep -qiE "Uninstalling unsloth-|Successfully installed unsloth" logs/update_venv.log; then
            echo "::error::pip ran anyway; refusing before the install changes did not work."
            grep -iE "Uninstalling|Successfully installed" logs/update_venv.log | tail -10
            exit 1
          fi

          # The install has to be exactly as usable as it was before the attempt.
          if [ ! -s "$SCRIPTS/unsloth.exe" ]; then
            echo "::error::no usable unsloth.exe left behind."
            ls -la "$SCRIPTS" | grep -i unsloth || true
            exit 1
          fi
          # Running at all is the property under test. The wreckage this prevents
          # is a stub that starts and then cannot import its own package, so the
          # CLI answering here is what proves unsloth_cli survived.
          #
          # Deliberately not studio_install_ok yet: the raw force-reinstall above
          # moved the installed version without re-running setup, so the manifest
          # is legitimately out of date, and the update that would refresh it is
          # the one just refused. Asserting it here would be asserting that a
          # refusal repaired something, which is the opposite of refusing.
          CAPS="$("$SCRIPTS/unsloth.exe" studio desktop-capabilities --json)" || {
            echo "::error::the CLI does not run after a refused update."
            exit 1
          }
          echo "$CAPS" | jq -e '.desktop_protocol_version' >/dev/null || {
            echo "::error::the CLI ran but produced nothing usable: $CAPS"
            exit 1
          }
          if ls "$SCRIPTS"/*.deleteme >/dev/null 2>&1; then
            echo "::error::a .deleteme orphan survived."
            ls -la "$SCRIPTS"/*.deleteme
            exit 1
          fi

          # And the update it refused must still work through the launcher, or the
          # advice it printed is useless. This is also where the install becomes
          # whole again, so studio_install_ok is asserted after it rather than
          # before.
          "$SHIM_UPD" studio update --local > logs/update_after_refusal.log 2>&1 || {
            echo "::error::the launcher route it points people at does not work."
            tail -40 logs/update_after_refusal.log
            exit 1
          }
          CAPS_AFTER="$("$SHIM_UPD" studio desktop-capabilities --json)" || {
            echo "::error::the CLI does not run after the launcher route completed."
            exit 1
          }
          echo "$CAPS_AFTER"
          echo "$CAPS_AFTER" | jq -e '.studio_install_ok == true' >/dev/null || {
            echo "::error::the launcher route completed but left the install damaged."
            exit 1
          }
          echo "a venv-launched replacement refuses safely and the launcher route repairs it"

      - name: Boot Unsloth briefly to confirm the install is still usable
        run: |
          mkdir -p logs
          UNSLOTH_API_ONLY=1 unsloth studio -H 127.0.0.1 -p 18891 \
            > logs/studio.log 2>&1 &
          PID=$!
          HEALTHY=""
          # Use jq (a Git Bash builtin) instead of `python -c
          # open('/tmp/health.json')` to read the saved health
          # response. Bash on windows-latest is MSYS Git Bash, which
          # resolves `/tmp/...` against the MSYS root, while the
          # python interpreter is Windows-native and resolves it
          # against the current drive's root. The two paths don't
          # agree, so python never finds the file curl just wrote.
          # jq reads through MSYS, so the path matches. Mirrors what
          # studio-windows-api-smoke.yml and the other Windows smoke
          # workflows already do.
          for i in $(seq 1 60); do
            if curl -fs http://127.0.0.1:18891/api/health > /tmp/health.json; then
              if jq -e '.status == "healthy"' /tmp/health.json >/dev/null; then
                HEALTHY=1
                break
              fi
            fi
            sleep 1
          done
          if [ -z "$HEALTHY" ]; then
            echo "Unsloth failed to come up after \`update\`"
            tail -200 logs/studio.log
            kill "$PID" 2>/dev/null || true
            exit 1
          fi
          kill "$PID" 2>/dev/null || true
          echo "post-update Unsloth /api/health OK"

      - name: Uninstall and verify clean
        # Round-trip through scripts/uninstall.ps1 against the default
        # install tree at %USERPROFILE%\.unsloth\studio. Catches
        # regressions where install.ps1 starts writing under a new key
        # (registry, Start Menu, %APPDATA%) and scripts/uninstall.ps1 has
        # not been updated to match. Skips gracefully if
        # scripts/uninstall.ps1 has not landed yet (lets this workflow
        # merge before #5513).
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path logs | Out-Null
          if (-not (Test-Path "$PWD\scripts\uninstall.ps1")) {
            Write-Host "scripts/uninstall.ps1 not present in this tree; skipping round-trip"
            "" | Set-Content logs/uninstall.log
            exit 0
          }
          pwsh -NoProfile -File "$PWD\scripts\uninstall.ps1" *>&1 | Tee-Object -FilePath logs/uninstall.log
          $leak = 0
          foreach ($p in @(
            "$env:USERPROFILE\.unsloth\studio",
            "$env:USERPROFILE\.unsloth\studio\unsloth_studio",
            "$env:USERPROFILE\.unsloth\studio\bin\unsloth.exe",
            "$env:USERPROFILE\.unsloth\whisper.cpp",
            "$env:USERPROFILE\.unsloth\.llama.cpp.install.lock",
            "$env:USERPROFILE\.unsloth\.node.install.lock",
            "$env:USERPROFILE\.unsloth\.whisper.cpp.install.lock"
          )) {
            if (Test-Path -LiteralPath $p) {
              Write-Host "::error::leak: $p"
              $leak++
            }
          }
          # Catch-all that does not need updating when a new artifact is added:
          # nothing but Unsloth writes under ~/.unsloth on a runner, so a
          # surviving directory means the uninstaller missed something. Mirrors
          # the POSIX check in studio-update-smoke.yml.
          if (Test-Path -LiteralPath "$env:USERPROFILE\.unsloth") {
            Write-Host "::error::leak: ~/.unsloth survived the uninstall"
            Get-ChildItem -LiteralPath "$env:USERPROFILE\.unsloth" -Force -Recurse -ErrorAction SilentlyContinue |
              Select-Object -First 40 -ExpandProperty FullName
            $leak++
          }
          if ($leak -gt 0) { exit 1 }
          # Idempotency.
          pwsh -NoProfile -File "$PWD\scripts\uninstall.ps1" *>&1 | Select-Object -Last 5
          pwsh -NoProfile -File "$PWD\scripts\uninstall.ps1" *>&1 | Select-Object -Last 5
          Write-Host "PASS: windows install -> update -> uninstall round-trip clean"

      - name: Upload update logs
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: windows-studio-update-log
          path: |
            logs/install.log
            logs/update.log
            logs/update2.log
            logs/studio.log
            logs/uninstall.log
          retention-days: 7
