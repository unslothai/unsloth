Continue “Merge all from main and fix merge conflicts.” The latest completed tool located #5704 conflict markers in `studio/backend/core/tool_healing.py`; do not re-run it. Next, inspect the conflicted sections in that file and edit the merge resolution so main’s `tool_call_parser.py` shim approach is preserved while #5704’s Mistral `[TOOL_CALLS]`/`[ARGS]` healing logic lands in `tool_healing.py`.