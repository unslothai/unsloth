Continue doing all fixes as part of the same PR, including patching other Unsloth Studio paths if needed beyond training. The latest completed tool action edited `src/features/chat/api/chat-api.ts`; do not re-run it. Next, proceed with the logical follow-up: continue applying the same needed patch pattern to the remaining identified Studio API paths or inspect the next already-identified file in the current review flow.