- User directed to check CI for PR `#5910`, specifically the clean-room Studio RAG PR.
- Scope decision: focus only on checks relevant to the PR changes: `Studio API & Auth Tests`, `Frontend build + bundle sanity`, `Tool calling Tests`, `Source lint`, `Repo tests (CPU)`, `Core`.
- Explicit command to run: `cd /mnt/disks/unslothai/ubuntu/unsloth/workspace_1/unsloth_rag_clean && gh pr checks 5910`.
- If any relevant check failed, investigate with `gh run view --job <id> --log-failed` or `gh api repos/unslothai/unsloth/actions/jobs/<id>/logs`, find the root cause in `studio-rag-clean`, fix it, run local RAG tests, commit, and push.
- Required local test command after fixes: `cd studio/backend && PYTHONPATH=$(pwd) python -m pytest tests/test_rag_*.py -q`.
- Rebase instruction: rebase onto `origin/studio-rag-clean` first if `pre-commit.ci` pushed auto-fixes; resolve conflicts by keeping the word-list locator implementation.
- Known exclusions: ignore pending checks, the known-flaky Windows upload-artifact issue, and the pre-existing datasets `IterableDataset` import error on base.
- Source lint handling decision: if `Source lint` is the only red check and only formatting, let `pre-commit.ci` auto-fix it unless it has been red for over 30 minutes.
- If all relevant checks are green or pending, report status briefly and do nothing else.
- Hard constraint: `Do NOT broadly pkill` because it causes `exit 144` and kills the running Studio on port `8950`.
- Hard constraint: `Do NOT start unrelated work`.
- COMPLETED: no commands or edits were performed in this span; only the user’s operational instructions were received.
- PENDING: run the requested CI check command and act according to relevant check status.