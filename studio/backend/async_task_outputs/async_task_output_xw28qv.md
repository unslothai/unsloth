Continue the latest directive to tighten the RAG-related comments/docstrings without changing behavior. Backend core edits and integration comment edits shown in the tail are already applied; ruff passed on `core/inference/tools.py`, `core/inference/llama_cpp.py`, `core/inference/safetensors_agentic.py`, and `routes/inference`. Next action is to proceed after the completed ruff check: mark the integration task complete if task tracking is still active, then inspect remaining changed files for overly verbose RAG comments and run the relevant validation once edits are done.