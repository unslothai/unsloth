Continue the latest directive: review/fix the PR findings, specifically F7 where MLX renders tools without the native-template fallback already present on the safetensors path. The completed work extracted native-template fallback logic into `chat_template_helpers.py`, refactored `InferenceBackend` to delegate to it, preserved the monkeypatchable `apply_fn`, and verified the existing native-template tests pass (`3 passed`). Next action: edit `studio/backend/core/inference/mlx_inference.py` to call the shared fallback when tools are requested and the MLX override render drops tool schemas, using `self.models[self.active_model_name]`, `self.active_model_name`, and the module `apply_chat_template_for_generation`.