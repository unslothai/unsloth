# Audio extras (installed with --no-deps --no-cache-dir)
descript-audio-codec
descript-audiotools
julius
torchcodec==0.10.0
snac

# peft 0.19.0 causes export subprocess shutdown issues in Studio;
# installing with --no-deps to avoid pulling in torch>=0.11.0
peft==0.18.1

# TRL and related packages
trl==0.23.1
git+https://github.com/meta-pytorch/OpenEnv.git
# executorch>=1.0.1               # 41.5 MB - no imports in unsloth/zoo/studio
torch-c-dlpack-ext
sentence_transformers==5.2.0
# macOS arm64: unsloth-zoo's mlx-vlm chain requires transformers>=5.1.0 (and
# >=5.5.0 for mlx-vlm 0.5.0). The base-update resolver picks a compatible
# version under --no-constraints; reinstalling 4.57.6 here with --no-deps
# would silently downgrade transformers and break mlx-vlm imports at runtime.
# constraints.txt still enforces 4.57.6 on every other platform.
transformers==4.57.6 ; sys_platform != "darwin" or platform_machine != "arm64"
pytorch_tokenizers
kernels==0.12.1
