# Single-env pins for unsloth + studio + data-designer
# Keep compatible with unsloth transformers bounds.
# macOS arm64: the unsloth-zoo -> mlx-vlm chain pulls transformers>=5.1.0
# (>=5.5.0 for mlx-vlm 0.5.0). Skip the 4.57.6 pin so later pip_install
# steps with -c constraints.txt do not silently re-downgrade transformers
# after the base step's --no-constraints fallback selected a 5.x version.
transformers==4.57.6 ; sys_platform != "darwin" or platform_machine != "arm64"
trl==0.23.1
huggingface-hub==0.36.2

# Studio stack
datasets==4.3.0
pyarrow==23.0.1

# FastMCP/OpenEnv compat
fastmcp>=3.0.2
mcp>=1.24,<2
websockets>=15.0.1

pandas==2.3.3
