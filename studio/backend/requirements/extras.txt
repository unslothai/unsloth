# transitive dep of onnxruntime (via data-designer's pymupdf4llm)
flatbuffers
# Also needed by sentence_transformers (installed with --no-deps in extras-no-deps.txt);
# librosa pulls it in too, but is skipped in no-torch mode.
scikit-learn==1.7.1

# Additional extras
jiwer                            # WER/CER metrics for vision OCR save-merge benchmarks
omegaconf
# einx 0.4.3 ships a top-level test/conftest.py. On Windows, pip can remove
# that shared path while replacing another wheel, and the updater embedded in
# desktop 0.1.901 then rejects the otherwise healthy install. 0.4.2 has the
# same runtime package without the colliding test tree. Keep this bridge for
# the 0.1.901 upgrade hop; newer verifiers ignore non-runtime wheel fixtures.
einx<0.4.3; sys_platform == "win32"
einx; sys_platform != "win32"
pyloudnorm
openai-whisper
av                               # PyAV: decode dictation audio (webm/opus/mp3/…) for the Whisper STT sidecar
uroman                           # 4.0 MB - used for Outetts.
MeCab                            # 19.9 MB - used for Outetts.
inflect                          # number-to-words, required by OuteTTS
loguru
flatten_dict
ffmpy
randomname
argbind
tiktoken
ftfy
importlib-resources
librosa
markdown2
matplotlib==3.10.9
pystoi
soundfile
tensorboard
torch-stoi
timm
einops
tabulate
openai>=2.7.2
websockets>=15.0.1
