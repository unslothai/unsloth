WORKER COMPLETE
===============
Branch: issue-4728-fix
Issue: #4728 - PyTorch 2.6.0 compatibility (recompile_limit removed)
Status: DONE

Changes Made:
- studio/backend/core/training/trainer.py: replaced unconditional
  `torch._dynamo.config.recompile_limit = 64` with hasattr-based
  feature detection that also sets `cache_size_limit` for PyTorch 2.1+.

Commit Message:
  Fix PyTorch 2.6.0 compatibility - handle recompile_limit removal

Testing:
- Verified the fix using a Python script simulating both attribute
  presence scenarios (cache_size_limit and recompile_limit).
- No existing test suite changes needed; fix is a pure guard-clause addition.

Ready for conductor review and integration.
